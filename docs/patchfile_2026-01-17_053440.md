--------------------------------------------
PATCH ID: 5
TITLE: Wire image render modes and toggles
FILES TOUCHED:
- docs/patchfile_2026-01-17_053440.md (created)
- ui/panels/images/images_tab.py (modified)
- app/app_run.py (modified)
- render/renderers/renderer.py (modified)
- render/renderers/renderer_image.py (modified)

SUMMARY:
- Added Images tab controls for render mode, scale, grid overlay, downsample, and preview size.
- Routed image render mode/toggles into renderer behavior, including height-field and overlay grid.
- Why it was changed: Implements image render modes/toggles wiring per docs/AUDIT_REPORT.md.

INVARIANTS CHECK:
- State ownership preserved? Yes; UI dispatches existing actions and renderer remains read-only.
- No new features added? Yes; wiring existing state fields.
- No UI behavior change? Yes; adds controls for existing fields.
- CLI not introduced yet? Yes.

DIFF / ACTIONS:
- Added Render Options section to Images tab.
- Renderer now respects show_image_on_grid, render_mode, and grid overlay.
- Sanity checks: `python3 -m compileall .`, `python3 main.py --help`.

STATUS:
- Patch complete
- Waiting for next instruction
--------------------------------------------
