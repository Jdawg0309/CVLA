--------------------------------------------
PATCH ID: 8
TITLE: Tie pipeline timeline to rendered image
FILES TOUCHED:
- docs/patchfile_2026-01-17_053953.md (created)
- app/app_run.py (modified)

SUMMARY:
- Rendered the current pipeline step output (or input) when stepping the timeline.
- Why it was changed: Makes timeline step index affect render output per docs/AUDIT_REPORT.md.

INVARIANTS CHECK:
- State ownership preserved? Yes; read-only render choice.
- No new features added? Yes.
- No UI behavior change? Yes; timeline now drives image preview.
- CLI not introduced yet? Yes.

DIFF / ACTIONS:
- App render loop now uses get_current_step output data when available.
- Sanity checks: `python3 -m compileall .`, `python3 main.py --help`.

STATUS:
- Patch complete
- Waiting for next instruction
--------------------------------------------
