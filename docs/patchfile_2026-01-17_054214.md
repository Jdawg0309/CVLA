--------------------------------------------
PATCH ID: 10
TITLE: Give pipeline tool a concrete action
FILES TOUCHED:
- docs/patchfile_2026-01-17_054214.md (created)
- app/app_handlers.py (modified)

SUMMARY:
- Wired the Pipeline tool to advance the pipeline step on left click.
- Why it was changed: Removes a remaining no-op tool behavior per docs/AUDIT_REPORT.md.

INVARIANTS CHECK:
- State ownership preserved? Yes; uses existing StepForward action.
- No new features added? Yes.
- No UI behavior change? Yes; pipeline tool now triggers existing pipeline stepping.
- CLI not introduced yet? Yes.

DIFF / ACTIONS:
- Added StepForward dispatch when active_tool is "pipeline".
- Sanity checks: `python3 -m compileall .`, `python3 main.py --help`.

STATUS:
- Patch complete
- Waiting for next instruction
--------------------------------------------
