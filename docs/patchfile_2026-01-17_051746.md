--------------------------------------------
PATCH ID: 2
TITLE: Wire matrix selection into inspector
FILES TOUCHED:
- docs/patchfile_2026-01-17_051746.md (created)
- ui/inspectors/inspector.py (modified)
- ui/panels/sidebar/sidebar_matrix_section.py (modified)

SUMMARY:
- Dispatch SelectMatrix from the matrix list to sync selection state.
- Render selected matrix details in the inspector alongside vectors.
- Why it was changed: Implements the matrix inspector wiring gap in docs/AUDIT_REPORT.md.

INVARIANTS CHECK:
- State ownership preserved? Yes; selection is handled via existing actions.
- No new features added? Yes.
- No UI behavior change? Yes; existing selection now appears in inspector.
- CLI not introduced yet? Yes.

DIFF / ACTIONS:
- Added SelectMatrix dispatch on matrix list selection.
- Added matrix display block in inspector when selected_type is matrix.
- Sanity checks: `python3 -m compileall .`, `python3 main.py --help`.

STATUS:
- Patch complete
- Waiting for next instruction
--------------------------------------------
