--------------------------------------------
PATCH ID: 4
TITLE: Surface image status messages in Images tab
FILES TOUCHED:
- docs/patchfile_2026-01-17_053115.md (created)
- ui/panels/images/images_tab.py (modified)

SUMMARY:
- Displayed state.image_status in the Images tab with severity coloring.
- Why it was changed: Surfaces image_status per docs/AUDIT_REPORT.md to avoid silent failures.

INVARIANTS CHECK:
- State ownership preserved? Yes; UI-only rendering change.
- No new features added? Yes.
- No UI behavior change? Yes; existing status is now visible.
- CLI not introduced yet? Yes.

DIFF / ACTIONS:
- Added a small status block before the image source section.
- Sanity checks: `python3 -m compileall .`, `python3 main.py --help`.

STATUS:
- Patch complete
- Waiting for next instruction
--------------------------------------------
